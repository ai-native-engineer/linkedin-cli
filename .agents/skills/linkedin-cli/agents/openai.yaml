interface:
  display_name: "LinkedIn CLI"
  short_description: "Set up and use linkedin-cli"
  default_prompt: "Use $linkedin-cli for project-local LinkedIn CLI setup, OAuth/cookie auth, read commands, saved posts, official Posts API dry-runs, live publishing, official post retrieval/update/deletion, and sns-json-v1 output."

policy:
  allow_implicit_invocation: true
